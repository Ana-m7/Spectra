# Base URL of the Spectra API (Node server), including the /api prefix.
# Leave unset for local development (defaults to http://localhost:5000/api).
# On Vercel, set this to your deployed Render API URL, e.g.:
# REACT_APP_API_URL=https://spectra-api.onrender.com/api
REACT_APP_API_URL=
